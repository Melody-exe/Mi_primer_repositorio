from setuptools import setup
setup(
    name='mipaquete', #no ti3ne q ser el mismo que paquete, usamos el nombre que querramos
    version='1.0', #la coma es IMPORTANTE.Asi todo puede estar en sun solo renglon 
    description='mi primer paquete redis',
    author='Franco',
    packages=['paquete'],

)